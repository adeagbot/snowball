package main.scala.dao;


object  BenchmarkDAO {
  
 import main.scala.model.{Benchmark=>BenchmarkObject} 
 
  def create(p:BenchmarkObject){
//    var query='INSERT INTO '+cf+' (id,benchmark_type,description,count,'+
//                        'subj_code,doc_type,pub_year,pub_month,period,'+
//                       'scores,percentiles,last_update) '+
//                       'VALUES (?,?,?,?,?,?,?,?,?,?,?,?)';  
  }
  def find(id:Int){
    
  }
  def update(p:BenchmarkObject){
//query='UPDATE '+cf+' SET code_version=?,scopus_last_update=?';    
  }
  def delete(id:Int)  {
//   query='DELETE FROM '+cf_eid+' WHERE scopus_eid=?'; 
  }
}
  


